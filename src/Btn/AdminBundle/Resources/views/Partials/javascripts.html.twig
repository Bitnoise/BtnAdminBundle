<!-- javascripts start -->
    {# load btn base admin assets #}
    {% btn_asset 'btn_admin_base_js' %}
        <script src="{{ asset_url }}"></script>
    {% endbtn_asset %}
    {# load assets from asset storage #}
    {% for asset in btn_get_assets('javascripts') %}
        {% btn_asset asset %}
            <script src="{{ asset_url }}"></script>
        {% endbtn_asset %}
    {% endfor %}
    <script>
        // register ready callback
        jQuery(function() {
            BtnApp.ready();
        })
        // init app
        BtnApp.init();
    </script>
<!-- javascripts end -->
