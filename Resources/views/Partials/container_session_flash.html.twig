{% from 'BtnAdminBundle:Macros:flash.html.twig' import session_flash %}
{% if app.session.flashbag.peekAll|length > 0 %}
    <div class="row">
        <div class="col-md-6 col-md-offset-3">{{ session_flash() }}</div>
    </div>
{% endif %}
