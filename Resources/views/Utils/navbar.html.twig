<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#header-menu">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{{ path('btn_admin_homepage') }}">{{ 'btn_admin.logo'|trans }}</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="header-menu">
        <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="{{ path('btn_admin_homepage') }}" class="dropdown-toggle" data-toggle="dropdown">
            {% trans %}btn_admin.loged_as{% endtrans %}
            {{ app.user ? app.user.username }}
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="{{ path('fos_user_profile_edit') }}"><span class="glyphicon glyphicon-user"></span> {{ 'btn_admin.profile'|trans }}</a></li>
            <li><a href="{{ path('fos_user_change_password') }}"><span class="glyphicon glyphicon-pencil" ></span> {{ 'btn_admin.change_password'|trans }}</a></li>
            <li><a href="{{ path('btn_admin_user_list') }}"><span class="glyphicon glyphicon-th-list"></span> {{ 'btn_admin.users_list'|trans}}</a></li>
            <li class="divider"></li>
            <li><a href="{{ path('fos_user_security_logout') }}"><span class="glyphicon glyphicon-off"></span> {{ 'btn_admin.logout'|trans }}</a></li>
          </ul>
        </li>
      </ul>

        {{ knp_menu_render(btn_admin.navbar_menu) }}

    </div><!-- /.navbar-collapse -->
</nav>
