{% macro breadcrumb(items) %}
    {% if items|length > 0 %}
        <ol class="breadcrumb breadcrumb-arrow">
            {% for url, name in items %}
                {% if loop.last is defined and loop.index == loop.last %}
                    <li class="active"><span>{{ name|trans }}</span></li>
                {% else %}
                    <li>
                        <a href="{{ path(url) }}">{{ name|trans }}</a>
                    </li>
                {% endif %}
            {% endfor %}
        </ol>
    {% endif %}
{% endmacro %}
