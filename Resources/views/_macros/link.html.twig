{% import _self as self %}

{% macro link(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_default(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-default">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_primary(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-primary">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_success(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-success">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_info(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-info">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_warning(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-warning">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_danger(label, route, route_params = {}) %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-danger">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_back(route, route_params = {}, label = 'btn_admin.back') %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-default btn-back">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_add(route, route_params = {}, label = 'btn_admin.add') %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-success btn-add">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_edit(route, route_params = {}, label = 'btn_admin.edit') %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-primary btn-edit">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_show(route, route_params = {}, label = 'btn_admin.show') %}
{% spaceless %}
    <a href="{{ path(route, route_params) }}" class="btn btn-primary btn-show">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

{% macro link_delete(route, route_params = {}, label = 'btn_admin.delete') %}
{% spaceless %}
    <a href="{{ csrf_path(route, route_params) }}" class="btn btn-danger btn-delete">
        {{ label|trans }}
    </a>
{% endspaceless %}
{% endmacro %}

