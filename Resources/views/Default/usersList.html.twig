{% extends 'BtnAdminBundle:Default:layout.html.twig' %}

{% block content %}
    <div class="fos_user_user_show"></div>
    <a href="{{ path('btn_admin_user_new') }}" class="btn btn-default"> <i class="glyphicon glyphicon-user"></i> {{ 'btn_control.user_add'|trans }}</a>
    <div class="table-responsive">
      <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>{{ 'btn_control.username'|trans }}</th>
                <th>{{ 'btn_control.email'|trans }}</th>
                <th>{{ 'btn_control.enabled'|trans }}</th>
                <th>{{ 'btn_control.last_login'|trans }}</th>
                <th>{{ 'btn_control.actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.username }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.enabled }}</td>
                    <td>{{ user.lastLogin|date('d-m-Y H:i:s') }}</td>
                    <td>
                        <a href="{{ path('btn_admin_user_edit', {id: user.id}) }}" class="btn btn-success">{{ 'btn_control.edit'|trans }}</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
      </table>
    </div>
{% endblock  %}
